#include <bits/stdc++.h>
using namespace std;
int N;
int main(void) {
	cin >> N;
	cout << 1999 << '\n';
	for (int i = 1; i < 1000; i++)
		cout << 1 << ' ';
	for (int i = 0; i < 1000; i++)
		cout << 1000 << ' ';
}