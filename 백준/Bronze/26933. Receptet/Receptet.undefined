#include <bits/stdc++.h>
using namespace std;

int N, H, B, K, res;

int main(void) {
	cin >> N;
	for (int i = 0; i < N; i++) {
		cin >> H >> B >> K;
		if (H < B)
			res += (B - H) * K;
	} cout << res;
}