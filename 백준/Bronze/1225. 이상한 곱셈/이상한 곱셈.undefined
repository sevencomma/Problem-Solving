#include <bits/stdc++.h>
using namespace std;

string a, b;
long long sum;

int main(void) {
	cin >> a >> b;

	for (int i = 0; i < a.length(); i++)
		for (int j = 0; j < b.length(); j++) {
			sum += (a[i] - '0') * (b[j] - '0');
		}
	cout << sum;
}