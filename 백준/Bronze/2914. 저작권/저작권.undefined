#include <bits/stdc++.h>
using namespace std;

int A, I;

int main(void) {
	cin >> A >> I;
	int x = 1;

	while (1) {
		if (ceil((double)x / A) == I) {
			cout << x;
			return 0;
		}
		x++;
	}
}