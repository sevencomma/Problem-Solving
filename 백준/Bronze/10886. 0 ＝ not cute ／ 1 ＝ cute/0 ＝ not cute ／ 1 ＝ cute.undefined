#include <cstdio>

int N, x;
int main(void) {
	scanf("%d", &N);
	int cnt = 0;
	for (int i = 0; i < N; i++) {
		scanf("%d", &x);
		cnt += x;
	}
	if (cnt * 2 >= N) puts("Junhee is cute!");
	else puts("Junhee is not cute!");
}