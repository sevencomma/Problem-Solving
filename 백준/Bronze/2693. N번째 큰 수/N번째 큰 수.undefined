#include <bits/stdc++.h>
using namespace std;

int T;

int main(void) {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);
	cin >> T;
	while (T--) {
		vector<int> a(10);
		for (int i = 0; i < 10; i++)
			cin >> a[i];
		sort(a.rbegin(), a.rend());
		cout << a[2] << '\n';
	}
}