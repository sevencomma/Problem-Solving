#include <bits/stdc++.h>
using namespace std;

string s;
int main(void) {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);

	while (1) {
		getline(cin, s);
		if (s == "END") break;
		reverse(s.begin(), s.end());
		cout << s << '\n';
	}
}