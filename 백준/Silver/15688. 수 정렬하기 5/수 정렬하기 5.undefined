#include <bits/stdc++.h>
using namespace std;

int N;

int main(void) {
	cin.tie(NULL);
	cout.tie(NULL);
	ios_base::sync_with_stdio(false);
	cin >> N;
	vector<int> num(N);
	for (int i = 0; i < N; i++)
		cin >> num[i];
	sort(num.begin(), num.end());
	for (auto& it : num)
		cout << it << '\n';
}