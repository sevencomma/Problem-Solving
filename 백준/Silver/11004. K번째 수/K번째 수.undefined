#include <bits/stdc++.h>
using namespace std;

vector<int> a;
int n, k;

int main(void) {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);
	cin >> n >> k;
	a.resize(n);
	for (int i = 0; i < n; i++)
		cin >> a[i];
	sort(a.begin(), a.end());
	cout << a[k - 1];
}