#include <bits/stdc++.h>
using namespace std;

int T, n, m, a, b;

int main(void) {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);
	cin >> T;
	while (T--) {
		cin >> n >> m;
		for (int i = 0; i < m; i++)
			cin >> a >> b;
		cout << n - 1 << '\n';
	}
}