#include <bits/stdc++.h>
using namespace std;

string s;
int cnt;

int main(void) {
	cin >> s;
	for (int i = 0; i < s.length() - 1; i++)
		if (s[i] != s[i + 1])
			cnt++;
	cout << (cnt + 1) / 2;
}